<!DOCTYPE html>

<html
  lang="en"
  data-wf-site="66e6ed97469869045902b70e"
  data-wf-page="66eaa6ae2a24a29c732b4aee">
  <head>
    <meta charset="utf-8">
    <title>
      {{ page_title }}
      {%- if current_tags %}
        &ndash; tagged "{{ current_tags | join: ', ' }}"{% endif -%}
      {%- if current_page != 1 %}
        &ndash; Page {{ current_page }}{% endif -%}
      {%- unless page_title contains shop.name %}
        &ndash; {{ shop.name }}{% endunless -%}
    </title>

    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="theme-color" content="">
    <link rel="canonical" href="{{ canonical_url }}">
    <meta name="generator" content="Shopify 2.0" />
    <link
      href="{{ 'normalize.css' | asset_url }}"
      rel="stylesheet"
      type="text/css">
    <link
      href="{{ 'shopify.css' | asset_url }}"
      rel="stylesheet"
      type="text/css">
    <link
      href="{{ 'shopify-template.css' | asset_url }}"
      rel="stylesheet"
      type="text/css">
    <script type="text/javascript">
      !function(o,c){var n=c.documentElement,t=" w-mod-";n.className+=t+"js",("ontouchstart"in o||o.DocumentTouch&&c instanceof DocumentTouch)&&(n.className+=t+"touch")}(window,document);
    </script>

    {%- if settings.favicon != blank -%}
      <link
        rel="icon"
        type="image/png"
        href="{{ settings.favicon | img_url: width: 32, height: 32 }}">
    {%- endif -%}
    {{ content_for_header }}

    {%- if settings.custom_head != blank -%}
      {{ settings.custom_head }}
    {%- endif -%}

    <style > :root
    {
      --secondary: {{ settings.color_secondary }};
    }
    .property-select {
      appearance: none !important;
    }
    .navbar-menu::-webkit-scrollbar {
      width: 0 !important;
    }
    .navbar-menu::-webkit-scrollbar-track {
      background: transparent !important;
    }
    .w-slider-dot {
      width: 0.75rem !important;
      height: 0.75rem !important;
      background-color: #adb4af !important;
    }
    .w-slider-dot.w-active {
      background-color: #083819 !important;
    }
  </style>
</head>
<body>
  <div class="page_wrapper">
    {% render 'header' %}
    <main class="main_wrapper">
      {{ content_for_layout }}
    </main>
    {% render 'footer' %}

  </div>

  <script
    src="{{ 'jquery-3.5.1.min.js' | asset_url }}"
    type="text/javascript"
    defer></script>
  <script src="{{ 'shopify.js' | asset_url }}" defer></script>
  <style>
    .JFUxW .cn-form-content {
      padding: 0 !important;
    }

    .nindo-form-label {
      display: none !important;
    }

    .nindo-input,
    .nindo-textarea {
      padding: 0.75rem 1rem !important;
      height: 100% !important;
    }
    .cn-form-buttons {
      margin: 0 !important;
    }
    .cn-form {
      max-width: none !important;
      background-color: transparent !important;
    }
  </style>
</body></html>